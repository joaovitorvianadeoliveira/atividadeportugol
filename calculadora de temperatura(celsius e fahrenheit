programa {
  funcao inicio() {
  
     //Declaração das variaveis a serem usadas
  caracter opcao //variavel que guarda a opção de operador escolhida
  real fator1 // variavel que guarda o fator de operação 1
  real fator2 // variavel que guarda o fator de operação 2
  real result // variavel que guarda o resultado
 logico reiniciar = verdadeiro //variavel lógica que guarada a opção de reiniciar ou não converte.
  enquanto(reiniciar){


  escreva("conversão de temperatura\n")
  escreva("\nEscolha a operação:\n")
  escreva("1 - celsius para fahrenheit\n")
  escreva("2 - Fahrenheit para celsius\n")
  escreva("\n0resposta:\t")
  leia(opcao)

  se (opcao==1){
    escreva("\nconversão de temperatura de celsius para fahrenheit:\n")
    escreva(" digite o primeiro fator1:\t\n")
    leia(fator1)
    result = fator1 * 1.8 + 32
escreva("\n0 resultado é:\t", result, "°F\n")}

se (opcao==2){
  escreva("\ncoversão de temperatur de fahrenheit para celsius:\n")
  escreva("digite o segundo fator2:\t\n")
  leia(fator2)
  result = (fator2-32)/1.8
  escreva("\n0 resultado é:\t" , result, "°C\n")}
    escreva("deseja converte novamente? verdadeiro para sim e falso para não\n")
    leia(reiniciar)
  }
}

  }
